var inspect = require('eyes').inspector({hideFunctions: true, maxLength: null});

module.exports = {
	show: {
		handler: function(request, reply) {
		}
	},
	create: {
		handler: function(request, reply) {
		}
	},
	update: {
		handler: function(request, reply) {
		}
	},
	destroy: {
		handler: function(request, reply) {
		}
	}
};
