div.set-group(data-hook="patterns-view-container")
	if patterns && patterns.length > 0
		h3 Patterns
		ul
			each pattern in patterns
				li
					- var destRoute = '/set/' + setName + '/pattern/' + pattern.name;
					a.pattern(data-dest-route="#{destRoute}" href="javascript:;") #{pattern.name}
	else
		if patterns
			h3 No patterns here
		//- else we're in loading or error state
