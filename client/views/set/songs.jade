div.view-group(data-hook="songs-view-container")
	if songs && songs.length > 0
		h3 Songs
		ul
			each song in songs
				li
					- var destRoute = slugger('/set/' + setName + '/song/' + song.name);
					a.client-nav(data-dest-route="#{destRoute}" href="#{destRoute}") #{song.name}
	else
		if songs
			h3 No songs here
		//- else we're in loading or error state
